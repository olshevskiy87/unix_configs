[alias]
    st = status -s -b # short mode for status
    br = branch
    co = checkout
    cb = checkout -b
    ci = commit
    lg = log --graph --pretty=format:'%C(yellow)%h%Creset -%C(green)%d%Creset %s %C(red)(%cr) %C(yellow)<%an>%Creset'
    shows = show --stat
[user]
    email = olshevskiy87@bk.ru
    name = Dmitriy Olshevskiy
[push]
    default = current
[color]
    ui = auto
[color "branch"]
    current = yellow reverse
    local = yellow
    remote = green
[color "diff"]
    meta = yellow bold
    frag = magenta bold
    old = red bold
    new = green bold
[color "status"]
    added = yellow
    changed = green
    untracked = cyan
[diff]
    tool = vimdiff # vim as default diff tool
    compactionHeuristic = true
[merge]
    tool = vimdiff
[include]
    path = ~/.gitconfig.custom
